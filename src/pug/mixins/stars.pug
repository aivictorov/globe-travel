//- +stars(5)

mixin stars(count)
    .stars 
        .stars__icons
            - let i = 0
            while (i < count)
                +use-svg(16, 16, '#star-icon')
                - i++
        .stars__text 
            | #{count} Star Hotel
